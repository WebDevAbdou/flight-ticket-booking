# System Flow Diagrams - Flight Booking System

## 📊 Complete Booking Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    FLIGHT BOOKING SYSTEM FLOW                    │
└─────────────────────────────────────────────────────────────────┘

1. USER REGISTRATION/LOGIN
   ┌──────────┐
   │  Guest   │
   └────┬─────┘
        │
        ├─── Register ──→ [Create Account] ──→ [Hash Password] ──→ [Save to DB]
        │                                                              │
        └─── Login ────→ [Verify Credentials] ──→ [Generate JWT] ←───┘
                                                         │
                                                         ▼
                                                   [Logged In User]

2. FLIGHT SEARCH
   ┌──────────────┐
   │ Logged User  │
   └──────┬───────┘
          │
          ├─── Browse Flights ──→ [Query Database] ──→ [Display Results]
          │                            │
          └─── Apply Filters ─────────┘
                 │
                 ├─ Origin
                 ├─ Destination
                 ├─ Date
                 └─ Price Range

3. BOOKING PROCESS
   [Select Flight]
          │
          ▼
   [Enter Passenger Details]
          │
          ▼
   [Create Booking] ──→ [Start Transaction]
          │                    │
          │                    ├─ Generate Booking Reference
          │                    ├─ Assign Seat Number
          │                    ├─ Reduce Available Seats
          │                    └─ Save Booking (Status: Pending)
          │
          ▼
   [Proceed to Payment]

4. PAYMENT PROCESS
   [Enter Payment Details]
          │
          ▼
   [Process Payment] ──→ [Start Transaction]
          │                    │
          │                    ├─ Generate Transaction ID
          │                    ├─ Save Payment Record
          │                    ├─ Update Booking Status (Confirmed)
          │                    └─ Create Receipt Record
          │
          ▼
   [Generate PDF Receipt]
          │
          ▼
   [Booking Complete!]

5. POST-BOOKING
   [View Dashboard]
          │
          ├─── View All Bookings
          ├─── View Receipt
          └─── Download PDF
```

---

## 🗄️ Database Transaction Flow

```
BOOKING TRANSACTION:
┌─────────────────────────────────────────────────────────────┐
│ BEGIN TRANSACTION                                            │
│                                                              │
│  1. Lock Flight Record (FOR UPDATE)                         │
│     SELECT * FROM flights WHERE flight_id = ? FOR UPDATE    │
│                                                              │
│  2. Check Seat Availability                                 │
│     IF available_seats > 0 THEN                             │
│                                                              │
│  3. Generate Booking Reference                              │
│     booking_ref = "BK" + random(8)                          │
│                                                              │
│  4. Generate Seat Number                                    │
│     seat = calculate_seat(total_seats, available_seats)     │
│                                                              │
│  5. Insert Booking                                          │
│     INSERT INTO bookings (...)                              │
│                                                              │
│  6. Update Available Seats                                  │
│     UPDATE flights SET available_seats = available_seats - 1│
│                                                              │
│  7. COMMIT                                                  │
│     ELSE                                                    │
│  8. ROLLBACK (No seats available)                           │
│                                                              │
└─────────────────────────────────────────────────────────────┘

PAYMENT TRANSACTION:
┌─────────────────────────────────────────────────────────────┐
│ BEGIN TRANSACTION                                            │
│                                                              │
│  1. Lock Booking Record (FOR UPDATE)                        │
│     SELECT * FROM bookings WHERE booking_id = ? FOR UPDATE  │
│                                                              │
│  2. Verify Booking Status = 'pending'                       │
│                                                              │
│  3. Generate Transaction ID                                 │
│     txn_id = "TXN" + timestamp + random                     │
│                                                              │
│  4. Insert Payment Record                                   │
│     INSERT INTO payments (...)                              │
│                                                              │
│  5. Update Booking Status                                   │
│     UPDATE bookings SET status = 'confirmed'                │
│                                                              │
│  6. Generate Receipt Number                                 │
│     receipt_num = "RCP" + random(10)                        │
│                                                              │
│  7. Insert Receipt Record                                   │
│     INSERT INTO receipts (...)                              │
│                                                              │
│  8. COMMIT                                                  │
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
REGISTRATION:
┌──────────────┐
│ User Submits │
│ Registration │
│    Form      │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│ Validate Input  │
│ - Email format  │
│ - Password len  │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Check Email     │
│ Not Exists      │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Hash Password   │
│ (bcrypt + salt) │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Save to DB      │
│ INSERT INTO     │
│ users (...)     │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Generate JWT    │
│ Token           │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Return Token    │
│ + User Data     │
└─────────────────┘

LOGIN:
┌──────────────┐
│ User Submits │
│ Login Form   │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│ Find User by    │
│ Email           │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Compare         │
│ Password Hash   │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Generate JWT    │
│ Token           │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Return Token    │
│ + User Data     │
└─────────────────┘

PROTECTED ROUTE ACCESS:
┌──────────────┐
│ API Request  │
│ with Token   │
└──────┬───────┘
       │
       ▼
┌─────────────────┐
│ Extract Token   │
│ from Header     │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│ Verify JWT      │
│ Signature       │
└──────┬──────────┘
       │
       ├─── Valid ──→ [Allow Access]
       │
       └─── Invalid ──→ [401 Unauthorized]
```

---

## 📁 File Structure Flow

```
CLIENT REQUEST
      │
      ▼
┌─────────────┐
│   Browser   │
│  (HTML/CSS/ │
│     JS)     │
└─────┬───────┘
      │
      │ HTTP Request
      │
      ▼
┌─────────────┐
│   Express   │
│   Server    │
│ (server.js) │
└─────┬───────┘
      │
      │ Route Matching
      │
      ▼
┌─────────────┐
│   Routes    │
│  (*.routes) │
└─────┬───────┘
      │
      │ Middleware
      │
      ▼
┌─────────────┐
│ Auth Check  │
│(middleware) │
└─────┬───────┘
      │
      │ If Authenticated
      │
      ▼
┌─────────────┐
│ Controllers │
│(*.controller│
│    .js)     │
└─────┬───────┘
      │
      │ Database Query
      │
      ▼
┌─────────────┐
│   MySQL     │
│  Database   │
└─────┬───────┘
      │
      │ Return Data
      │
      ▼
┌─────────────┐
│   JSON      │
│  Response   │
└─────┬───────┘
      │
      ▼
┌─────────────┐
│  Browser    │
│  Updates    │
│    UI       │
└─────────────┘
```

---

## 🔄 Data Flow Example: Book a Flight

```
1. USER ACTION
   User clicks "Book Now" on Flight #101
   
2. FRONTEND (booking.js)
   ├─ Get flight ID from URL
   ├─ Fetch flight details from API
   ├─ Display flight information
   └─ Show passenger form

3. USER FILLS FORM
   ├─ Passenger Name: "John Doe"
   ├─ Email: "john@example.com"
   └─ Phone: "555-1234"

4. FORM SUBMISSION
   Frontend sends POST request:
   POST /api/bookings
   Headers: { Authorization: "Bearer <token>" }
   Body: {
     flightId: 101,
     passengerName: "John Doe",
     passengerEmail: "john@example.com",
     passengerPhone: "555-1234"
   }

5. BACKEND (booking.routes.js)
   ├─ Route: POST /api/bookings
   └─ Middleware: authenticateToken()

6. MIDDLEWARE (auth.middleware.js)
   ├─ Extract JWT token
   ├─ Verify token
   ├─ Add user info to request
   └─ Call next()

7. CONTROLLER (booking.controller.js)
   ├─ Start database transaction
   ├─ Lock flight record
   ├─ Check seat availability
   ├─ Generate booking reference: "BK12345678"
   ├─ Calculate seat number: "15C"
   ├─ Insert booking record
   ├─ Update available seats
   └─ Commit transaction

8. DATABASE (MySQL)
   ├─ INSERT INTO bookings (...)
   ├─ UPDATE flights SET available_seats = available_seats - 1
   └─ Return booking ID

9. RESPONSE
   Backend sends JSON:
   {
     success: true,
     booking: {
       bookingId: 42,
       bookingReference: "BK12345678",
       seatNumber: "15C",
       amount: 299.99
     }
   }

10. FRONTEND
    ├─ Receive response
    ├─ Store booking ID
    └─ Redirect to payment page
```

---

## 🎯 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                     PRESENTATION LAYER                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │   HTML   │  │   CSS    │  │JavaScript│  │  Browser │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTP/HTTPS
                         │
┌────────────────────────▼────────────────────────────────────┐
│                    APPLICATION LAYER                         │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Routes  │  │Middleware│  │Controller│  │  Utils   │   │
│  │          │  │          │  │          │  │          │   │
│  │ Express  │  │   JWT    │  │ Business │  │   PDF    │   │
│  │  Router  │  │   Auth   │  │  Logic   │  │Generator │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
└────────────────────────┬────────────────────────────────────┘
                         │ SQL Queries
                         │
┌────────────────────────▼────────────────────────────────────┐
│                      DATA LAYER                              │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │
│  │  Users   │  │ Flights  │  │ Bookings │  │ Payments │   │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │
│  ┌──────────┐  ┌──────────┐                                │
│  │ Receipts │  │ Contacts │                                │
│  └──────────┘  └──────────┘                                │
│                                                              │
│                    MySQL Database                            │
└─────────────────────────────────────────────────────────────┘
```

---

This visual guide helps understand how all components work together!

